<?php
/**
 * Git で管理する追加クライアント名簿（任意）
 * -----------------------------------------------------------------------------
 * 「クライアントボイス投稿を持たないクライアント」をここに書くと、
 * トップページのエリア別モーダルに追加表示されます。
 * ダミー投稿を作る必要はありません。
 *
 * ・キー   = 都道府県スラッグ（template/data/client-areas.php と同じもの）
 * ・値     = クライアントの配列
 *
 * 書き方は 2 通り。会社名の末尾の「様」は自動で付くので不要です。
 *
 *   'hokkaido' => array(
 *     '株式会社ウッディークラフト',                        // リンク無しで表示
 *     array( 'name' => '篠河建設㈱', 'url' => 'https://...' ), // リンク付きで表示
 *   ),
 *
 * 記事（クライアントボイス）がある会社は WordPress 側で自動的に載るので、
 * ここに重複して書く必要はありません。万一同名が両方にあった場合は
 * 記事側が優先され、こちらは表示されません。
 *
 * ※ このファイルを編集してコミット／デプロイすれば即反映されます。
 */

return array(

	// 例）
	// 'hokkaido' => array(
	// 	'株式会社ウッディークラフト',
	// ),

);
